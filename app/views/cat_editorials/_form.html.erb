<%= form_with(model: cat_editorial, local: true) do |form| %>
  <% if cat_editorial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cat_editorial.errors.count, "error") %> prohibited this cat_editorial from being saved:</h2>

      <ul>
      <% cat_editorial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :catalogue_id %>
    <%= form.number_field :catalogue_id %>
  </div>

  <div class="field">
    <%= form.label :link_url %>
    <%= form.text_field :link_url %>
  </div>

  <div class="field">
    <%= form.label :img_url %>
    <%= form.text_field :img_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
