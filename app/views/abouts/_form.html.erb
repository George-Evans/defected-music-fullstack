<%= form_with(model: about, local: true) do |form| %>
  <% if about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(about.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% about.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row">
    <div class="col-3"><%= form.label :title %>*</div>
    <div class="col-9"><%= form.text_field :title %></div>
  </div>

  <div class="field row">
    <div class="col-3"><%= form.label 'Body Text' %>*</div>
    <div class="col-9"><%= form.text_area :body %></div>
  </div>

  <div class="field row">
    <div class="col-3"><%= form.label 'Image' %>*</div>
    <div class="col-9"><%= form.text_field :img_url %></div>
  </div>

  <div class="actions row">
    <div class="col-3"></div>
    <div class="col-9"><%= form.submit %></div>
  </div>
<% end %>
